node_modules/
dist/
.astro/
.wrangler/
.dev.vars
.DS_Store
*.log
# .env holds only PUBLIC_SITE_URL, no secrets, so it is committed
# .env
.env.*
reference/*.docx

# Generated image sources, large PNGs kept locally for re-encoding
src/assets/images/_originals/
